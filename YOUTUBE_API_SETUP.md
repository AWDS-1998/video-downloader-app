# 🎬 دليل إعداد YouTube Data API للتطبيق

## المشكلة الحالية

| الطريقة | سرعة جلب البيانات | سرعة التحميل |
|---------|-------------------|--------------|
| **yt-dlp (الحالي)** | ❌ 85+ ثانية | ✅ يعمل |
| **YouTube Data API (المقترح)** | ✅ 0.3 ثانية | ✅ يعمل (yt-dlp للتحميل فقط) |

## الاستراتيجية الهجينة

```
📱 المستخدم يلصق رابط
     ↓
🚀 YouTube API → بيانات الفيديو فوراً (< 0.5 ثانية)
     ↓
📱 المستخدم يختار الجودة ويضغط تحميل
     ↓
⚙️ yt-dlp → استخراج رابط التحميل المباشر (مرة واحدة)
     ↓
📱 الجوال يحمّل مباشرة من YouTube CDN
```

---

## ✅ الخطوات المطلوبة منك (3 خطوات فقط)

### الخطوة 1: إنشاء مشروع Google Cloud

1. افتح: https://console.cloud.google.com/
2. سجّل دخول بحساب Google
3. اضغط **"Create Project"** (إنشاء مشروع)
4. اسم المشروع: `MisbarTube` (أو أي اسم تريده)
5. اضغط **"Create"**

### الخطوة 2: تفعيل YouTube Data API

1. من القائمة الجانبية، اذهب إلى **"APIs & Services"** → **"Library"**
2. ابحث عن **"YouTube Data API v3"**
3. اضغط عليها ثم اضغط **"Enable"** (تفعيل)

### الخطوة 3: إنشاء API Key

1. اذهب إلى **"APIs & Services"** → **"Credentials"**
2. اضغط **"+ CREATE CREDENTIALS"** → **"API Key"**
3. سيظهر لك مفتاح مثل: `AIzaSyBxxxxxxxxxxxxxxxxxxxxxxxxx`
4. **انسخ المفتاح وأرسله لي**

> [!IMPORTANT]
> أرسل لي المفتاح فقط وأنا أضيفه في السيرفر تلقائياً

---

## 💰 هل هو مجاني؟

**نعم، مجاني تماماً!**

| الميزة | التفاصيل |
|--------|----------|
| **الكوتا اليومية** | 10,000 وحدة / يوم |
| **تكلفة جلب بيانات فيديو** | 1 وحدة |
| **يعني كم فيديو/يوم؟** | ~10,000 فيديو |
| **بطاقة ائتمان؟** | ❌ لا يحتاج |
| **مدة صلاحية؟** | ♾️ لا تنتهي |

> [!NOTE]
> 10,000 طلب يومياً كافية لآلاف المستخدمين لأننا نستخدم كاش (نفس الفيديو لا يُطلب مرتين)

---

## 🔧 ماذا سأفعل أنا بعد إرسالك المفتاح؟

1. أضيف المفتاح في ملف إعدادات السيرفر
2. أعدّل الـ Backend ليستخدم YouTube API لجلب البيانات (عنوان، صورة، مدة، إلخ)
3. أبقي yt-dlp **فقط** لاستخراج رابط التحميل المباشر عند الضغط على "تحميل"
4. أضيف كاش ذكي لتقليل استهلاك الكوتا

---

## 📊 النتيجة المتوقعة

| العملية | قبل | بعد |
|---------|------|------|
| جلب بيانات الفيديو | 85 ثانية | **0.3 ثانية** |
| بدء التحميل | 85 ثانية | **10-30 ثانية** (مرة واحدة فقط) |
| تحميل مكرر (كاش) | 85 ثانية | **فوري** |
